<Page
    x:Class="Arqanum.Pages.DefaultChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:local="using:Arqanum.Controls" 
    xmlns:controls="using:YourApp.Controls">

    <Grid
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
            <Image Source="/Assets/Images/logo.png" Width="100" Height="100" Stretch="Uniform"/>
            <TextBlock
                Text="Welcome to Arqanum"
                FontSize="20"
                FontWeight="Bold"
                Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                TextAlignment="Center" />
            <TextBlock
                Text="Some text"
                FontSize="15"
                FontWeight="Normal"
                Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                TextAlignment="Center" />


            <!--<Grid x:Name="RootGrid">
                <StackPanel Spacing="8">
                    <TextBox x:Name="MessageBox" Width="300"/>
                    <Button Content="😀" Click="EmojiButton_Click"/>
                </StackPanel>

            </Grid>-->

        </StackPanel>
    </Grid>
</Page>
